const generateCourseSchedule = async (sched_name, term_course_ids, filter, res) => {
    // get sections based on term_course_ids

    // generate schedules

    // pick one

    // add schedule to database with given sched_Name

    // respond to request with chosen schedule
}

module.exports = generateCourseSchedule;